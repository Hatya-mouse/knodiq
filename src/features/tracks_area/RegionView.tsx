export default function RegionView({
    className = "",
}: {
    className?: string,
}) {
    return (
        <div className={`h-full w-96 default-border bg-amber-500 ${className}`}>

        </div>
    )
}